[nix]
channel = "stable-24_11"

run = "npm install && npm run dev -- -H 0.0.0.0 -p 3000"

[agent]
expertMode = true

[workflows]
runButton = "Dev"

[[workflows.workflow]]
name = "Dev"
mode = "sequential"
author = 45527199

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "npm install && npm run dev -- -H 0.0.0.0 -p 3000"

[[ports]]
localPort = 3000
externalPort = 80
